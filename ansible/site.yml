---
- name: Install Net Tools
  hosts: webservers
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install net-tools
      apt:
        name: net-tools
        state: present